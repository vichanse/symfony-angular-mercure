import { environment } from '../environments/environment';

export const APP_CONFIG = {
    apiWineUrl: environment.apiWineUrl,
    baseUrl: environment.baseUrl,
    merculeHubUrl: environment.merculeHubUrl,
};
